# レビュー: 施設ローカルマップ × GPS デモ 仕様書

- 日付: 2026-07-13
- 種別: 仕様書レビュー
- 対象: [仕様書](../specs/2026-07-13_local-map-gps-demo.md)
- ファイル名: `2026-07-13_local-map-gps-demo_spec-review_r2.md`
- 実行指定AI / モデル / effort: Claude Code / sonnet / medium
- 実行確認AI / モデル / effort: Claude Code / claude-sonnet-5 / medium
  （modelUsage: claude-sonnet-5 主）
- ラウンド: 2

## 総評(3行以内)

r1のMust(デモ位置ボタン表示条件の矛盾)、Should3件(最小二乗法の明記、pages.ymlの変更点明記、Node/npm固定方式の統一)はいずれも仕様書へ反映され、矛盾なく整合している。最小二乗法の追加に伴い、10章のテスト戦略に対応するunitケースが未記載である点のみ新たに指摘するが、実装を阻害する水準ではないため承認とする。

## 判定
承認

## 指摘事項

### Must(必ず対応)

(なし)

### Should(対応推奨)

- [ ] **4-3で追加した「基準点が3点を超える場合の最小二乗近似」に対応するunitテストケースが10章のテスト戦略に未記載。** 根拠: 4-3は「基準点が3点を超える場合は、全基準点に対する残差の二乗和を最小化する最小二乗法でアフィン変換係数を近似する」とr1のShouldを受けて明記されたが、10章「射影計算」のテストケース列は「基準点一致、画像中央、画像外、基準点不足・重複・一直線」のままで、4点以上の過剰決定系での近似成功ケースが含まれていない。初回実装が仮データ3点のみで進む場合は実害がないが、現地キャリブレーションで4点以上に増やす運用(未決事項に記載あり)を見込むなら、実装時にこのケースのunit testが漏れないよう一文加えることを推奨する。修正案: 10章「射影計算」の主要ケースへ「基準点4点以上(最小二乗近似)」を追加する。

### Could(任意・改善提案)

- [ ] r1のCould「HTML/SVGまたはCanvasの両論併記」「精度不足の閾値未定」は今回も未反映だが、いずれも実装時の裁量・現地実測待ちの事項であり、再指摘は見送る。

## 良い点

- r1のMust(デモ位置ボタンの表示条件矛盾)は、画面構成(3章)・4-6・M1完了条件のすべてで「状態によらず常時表示、異常状態では視覚的に強調」に統一されており矛盾が解消されている。判断理由も14章の自律判断ログへ明記されている。
- r1のShould3件(最小二乗法の算出方法、pages.ymlの`paths:`追加とbuild/コピー処理、Node/npm固定方式の`camera-ar-gomi-demo`への統一)がいずれも該当節へ具体的に反映されており、記載内容は実際の`.github/workflows/pages.yml`の構成(paths指定・setup-node・build・public/へのコピー)と矛盾しない。
- 反映にあたっての判断根拠(Node/npm方式の統一理由など)が自律判断ログに残されており、後工程で迷いにくい。

## Git反映メモ

- 承認の場合: 仕様書(`docs/specs/2026-07-13_local-map-gps-demo.md`)と関連ドキュメント一式をcommit対象としてよい(push方針は`docs/status.md`に従う)。

## 補足

- Should項目(テストケース追記)はM2/M3着手前にCodexが10章へ一文加えるだけで解消できる軽微な事項であり、承認判定を妨げるものではないと判断した。
- 対象仕様書・r1レポート・アイデア・決定メモに加え、実際の`.github/workflows/pages.yml`の現状を突き合わせて、pages.yml関連のShould反映が実態と整合しているかを確認した。
