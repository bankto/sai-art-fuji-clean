{
  "name": "local-map-gps-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "node scripts/preview-dist.mjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --test-isolation=none test/affine.test.ts test/geolocation.test.ts test/zones.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
