# TF.js model directory

Teachable Machine の画像分類モデルを使う場合は、このディレクトリに次のファイルを置く。

- `model.json`
- `metadata.json`
- `weights.bin` またはエクスポートに含まれる shard ファイル

未配置の場合、アプリはカメラフレーム由来の簡易ハッシュを使う内部フォールバックで、ラベルと信頼度を自動認識としてシミュレートする。モデル有無による操作差はなく、どちらもカメラ起動後に認識結果が自動更新される。UI上にデモ表記や手動ラベル選択は出さない。
