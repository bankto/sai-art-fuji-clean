# TF.js model directory

Teachable Machine の画像分類モデルを使う場合は、このディレクトリに次のファイルを置く。

- `model.json`
- `metadata.json`
- `weights.bin` またはエクスポートに含まれる shard ファイル

未配置の場合、アプリはデモモードで起動し、手動ラベル選択から M1〜M2 のカメラ、生成、URL/QR 持ち帰りフローを確認できる。
